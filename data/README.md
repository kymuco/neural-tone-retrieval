# Data Directory

This directory is reserved for small tracked metadata, schemas, and documentation related to datasets.

Large raw audio, intermediate renders, and processed corpora should stay outside version control by default.

Expected local-only subdirectories:

- `raw/`
- `interim/`
- `processed/`
